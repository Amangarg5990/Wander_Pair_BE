# Server Port (assigned dynamically on Render/Railway/Fly.io)
PORT=3000

# Frontend URL(s) for CORS (comma-separated if multiple, or * for all)
# Example: https://wanderpair.vercel.app,http://localhost:5173
FRONTEND_URL=*
